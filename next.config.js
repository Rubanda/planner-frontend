/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "public.blob.vercel-storage.com",
          "images.unsplash.com",
        ],
    },
}

module.exports = nextConfig
